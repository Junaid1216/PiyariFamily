// Tab bar visibility is handled centrally in MainTabNavigator via tabBarVisibility.ts.
// This hook is kept for compatibility with screens that already call it.
export const useHideTabBar = () => {};
